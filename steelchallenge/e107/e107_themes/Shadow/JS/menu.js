window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('pagina1_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});

window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('pagina2_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});
window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('pagina3_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});

window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('cerca_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});

window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('contatti_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});

window.addEvent('domready', function(){
	

	// Second Example
	
	// The same as before: adding events
	$('logged_tab').addEvents({
		'mouseenter': function(){
			// Always sets the duration of the tween to 1000 ms and a bouncing transition
			// And then tweens the height of the element
			this.set('tween', {
				duration: 2000,
				transition: Fx.Transitions.Bounce.easeOut // This could have been also 'bounce:out'
			}).tween('height', '210px');
		},
		'mouseleave': function(){
			// Resets the tween and changes the element back to its original size
			this.set('tween', {duration: 1400}).tween('height', '30px');
		}
	});
});

